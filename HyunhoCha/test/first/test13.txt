반복문 작성시 나타나는 어셈블리의 특성은 이전 문제에 나왔던 if문의 동작과 유사하다.

int i;

for(i=0; i < 10; i++)

위의 코드를 예로들면 먼저 for문을 진입할때

cmp(10, i) 같이 i 값과 10을 cmp문을 통해 비교한다.

그다음

jne(같지 않으면 분기)같은 분기문을 통해 분기한후 for문안의 로직을 실행한다.

그 후 i값을 한개 더하여 다시한번 cmp(10, i)와 같은 비교를 하고 조건분기문을 실행한다.

반복문은 위와같이 반복한다.


